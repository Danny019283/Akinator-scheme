[project]
name = "akinator-frontend"
version = "0.2.0"
description = "Frontend web (Streamlit) para el motor Akinator en Racket"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.35",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
